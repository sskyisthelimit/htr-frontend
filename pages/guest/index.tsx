import React from "react";
import ChatInstance from "../../src/components/ChatInstance";

const Home: React.FC = () => {
  return (
      <ChatInstance settedFirstSubmit={false}/>  );
};

export default Home;
